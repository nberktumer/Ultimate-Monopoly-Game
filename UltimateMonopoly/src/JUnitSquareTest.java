import static org.junit.Assert.*;

import org.junit.Test;

import domain.Player;
import domain.square.GoToJailSquare;
import domain.square.IncomeTaxSquare;
import domain.square.ReverseDirectionSquare;

public class JUnitSquareTest {
	/*
	 * @Test public void isinJailwhenInJail() { Player pl = new Player("trial",
	 * 3200, false, null, null, 1, 3, false); GoToJailSquare goJ = new
	 * GoToJailSquare("name"); goJ.landedOn(pl); assertTrue(pl.isInJail()); }
	 * 
	 * @Test public void isReverseWhenDirectionIsPositive() { Player pl = new
	 * Player(); pl.setDirection(1); ReverseDirectionSquare rDir = new
	 * ReverseDirectionSquare("name"); rDir.landedOn(pl); assertEquals(-1,
	 * pl.getDirection()); }
	 * 
	 * @Test public void isReverseWhenDirectionIsNegative() { Player pl = new
	 * Player(); pl.setDirection(-1); ReverseDirectionSquare rDir = new
	 * ReverseDirectionSquare("name"); rDir.landedOn(pl); assertEquals(1,
	 * pl.getDirection()); }
	 * 
	 * @Test public void isIncomeTaxTrueWhenPlayerHasMoreThanThreshold() {
	 * Player pl = new Player("trial", 3200, false, null, null, 1, 3, false);
	 * IncomeTaxSquare iTax = new IncomeTaxSquare("name"); iTax.landedOn(pl);
	 * assertEquals(3000, pl.getBalance()); // assertEquals(expected,actual) }
	 * 
	 * @Test public void isIncomeTaxTrueWhenPlayerHasLessThanThreshold() {
	 * Player pl = new Player("trial", 1000, false, null, null, 1, 3, false);
	 * IncomeTaxSquare iTax = new IncomeTaxSquare("name"); iTax.landedOn(pl);
	 * assertEquals(900, pl.getBalance()); }
	 */
}
